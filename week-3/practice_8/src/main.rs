fn main() {
    let fees = 25_000;
    println!("fees is {}", fees);
    
    // fees = 35_000; // Commented out to prevent the "cannot assign twice to immutable variable" error
    // println!("fees changed is {}", fees);
}